<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    
    <application
        android:label="vaseela"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- ══ INTENT-BASED (URI scheme detection) ══ -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="jazzcash" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="easypaisa" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="sadapay" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="nayapay" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="hblmobile" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="meezanbank" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="ubldigital" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="mcblive" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="myabl" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="alfalfahbank" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.support.customtabs.action.CustomTabsService" />
        </intent>

        <!-- ══ PACKAGE-BASED (verified Google Play Store IDs) ══ -->
        <!-- Mobile Wallets / EMIs -->
        <package android:name="com.techlogix.mobilinkcustomer" />   <!-- JazzCash -->
        <package android:name="pk.com.telenor.phoenix" />            <!-- Easypaisa -->
        <package android:name="com.sadapay.sadapay" />               <!-- SadaPay -->
        <package android:name="com.nayapay.app" />                   <!-- NayaPay -->

        <!-- Major Commercial Banks -->
        <package android:name="com.hbl.android.hblmobilebanking" />  <!-- HBL Mobile (verified) -->
        <package android:name="invo8.meezan.mb" />                   <!-- Meezan Bank (verified) -->
        <package android:name="app.com.brd" />                       <!-- UBL Digital (verified) -->
        <package android:name="com.mcb.mcblive" />                   <!-- MCB Live (verified) -->
        <package android:name="com.ofss.digx.mobile.android.allied" /> <!-- myABL Allied (verified) -->
        <package android:name="com.base.bankalfalah" />              <!-- Alfa / Bank Alfalah (verified) -->

        <!-- Mid-tier & Islamic Banks -->
        <package android:name="com.bankalhabib.mobile" />            <!-- Bank AL Habib -->
        <package android:name="com.faysalbank.mobilebanking" />      <!-- Faysal Bank -->
        <package android:name="com.nbp.mobilebanking" />             <!-- NBP Digital -->
        <package android:name="com.askaribank.mobilebanking" />      <!-- Askari Bank -->
        <package android:name="com.jsbank.mobilebanking" />          <!-- JS Bank -->
        <package android:name="com.bankislami.mobilebanking" />      <!-- BankIslami -->
        <package android:name="com.soneribank.mobile" />             <!-- Soneri Bank -->
        <package android:name="com.scb.bma.android" />              <!-- Standard Chartered -->
        <package android:name="com.dubaiislamicbank.mobile" />       <!-- Dubai Islamic Bank -->
        <package android:name="com.habibmetro.mobilebanking" />      <!-- Habib Metro -->

        <!-- Legacy packages (fallback) -->
        <package android:name="com.hbl.mobilebanking" />
        <package android:name="com.meezan.mobilebanking" />
        <package android:name="com.ubl.mobileapp" />
        <package android:name="com.mcb.mobilebanking" />
        <package android:name="com.abl.mobilebanking" />
        <package android:name="com.bankalfalah.mobile" />
    </queries>
</manifest>
